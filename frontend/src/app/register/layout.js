export const metadata = {
  title: 'Registro de cliente',
  description: 'Registro público de clientes para crear solicitudes de soporte en TecHelpDesk.',
  robots: {
    index: true,
    follow: true,
  },
};

export default function RegisterLayout({ children }) {
  return children;
}
