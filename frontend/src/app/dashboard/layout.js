export const metadata = {
  title: 'Dashboard',
  description: 'Panel privado de TecHelpDesk para consultar accesos y resumen del usuario autenticado.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function DashboardLayout({ children }) {
  return children;
}
