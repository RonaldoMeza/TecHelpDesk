export const metadata = {
  title: 'Iniciar sesión',
  description: 'Acceso de usuarios al sistema TecHelpDesk mediante credenciales seguras.',
  robots: {
    index: true,
    follow: true,
  },
};

export default function LoginLayout({ children }) {
  return children;
}
