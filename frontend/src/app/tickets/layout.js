export const metadata = {
  title: 'Tickets',
  description: 'Modulo privado para gestionar tickets, incidencias, responsables e historial en TecHelpDesk.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function TicketsLayout({ children }) {
  return children;
}
