export const metadata = {
  title: 'Crear ticket',
  description: 'Formulario privado para registrar nuevas incidencias en TecHelpDesk.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function NewTicketLayout({ children }) {
  return children;
}
